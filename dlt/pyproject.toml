[project]
name = "dlt-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dlt[workspace]>=1.22.2",
    "numpy>=2.4.2",
    "pandas>=3.0.1",
]
